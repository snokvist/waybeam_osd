#ifndef _GNU_STUBS_SOFT_H
#define _GNU_STUBS_SOFT_H 1
/*
 * Fallback stub header for toolchains that omit glibc's generated gnu/stubs-soft.h.
 * The Sigmastar SDK does not rely on any __stub_* markers, so an empty guard
 * satisfies the include chain pulled in by stdio/libc headers.
 */
#endif
